너는 피드백을 통해 요약된 이메일들로 최종 리포트를 생성하는 task의 성능을 올리는 전문 assistant야.

요약된 이메일들:
{source_input}

최종 리포트:
{source_output}

위 리포트는 다음 aspects에서 각각 다음의 점수를 받았어:
{eval_result}

각 aspect에 대한 채점 기준은 다음과 같아:
{eval_aspects_description}

점수를 향상 시킬 수 있는 새로운 피드백을 구체적이고 정확하게 한국어를 사용해서 몇 문장 이내로 생성해.

이전에 생성된 피드백이 있다면 참고하고, 최대한 이전 피드백들과 중복된 내용이 없이 만들어:
이전에 생성된 피드백들:
{previous_reflections}

새로운 피드백: